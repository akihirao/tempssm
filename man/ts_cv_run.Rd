% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_cross_validation.R
\name{ts_cv_run}
\alias{ts_cv_run}
\title{Run time series cross-validation over multiple folds}
\usage{
ts_cv_run(
  folds,
  ar_order = 1,
  use_season = TRUE,
  parallel = TRUE,
  workers = future::availableCores(),
  progress = FALSE,
  marginal = TRUE
)
}
\arguments{
\item{folds}{A list of fold objects returned by \code{ts_train_test_split()}.}

\item{ar_order}{Integer scalar specifying the order of the autoregressive
(AR)
component in the error structure (e.g., 2 for AR(2), 3 for AR(3)).
Defaults to 1. Orders from 1 to 4 are intended as the usual working range;
larger values are allowed but trigger a warning because they may lead to
unstable estimation.}

\item{use_season}{Logical scalar; should the seasonal component be
considered? Defaults to \code{TRUE}.}

\item{parallel}{Logical scalar; if \code{TRUE}, folds are evaluated in parallel using the
\pkg{future.apply} framework. If \code{FALSE}, folds are processed
sequentially. Default is \code{TRUE}.}

\item{workers}{Integer scalar specifying the number of parallel workers to use when
\code{parallel = TRUE}. The default uses all available cores as
returned by \code{\link[future]{availableCores}}.}

\item{progress}{Logical scalar; if \code{TRUE}, a progress bar is displayed during execution
using the \pkg{progressr} package. If \code{FALSE}, no progress bar
is shown. Default is \code{FALSE}.}

\item{marginal}{Logical scalar specifying the likelihood used during
parameter estimation. If \code{TRUE} (the default), KFAS uses the
marginal likelihood, which adds the diffuse-initialization correction
term. If \code{FALSE}, KFAS uses the diffuse likelihood. The selected
setting is stored in the fitted object and used by default by
\code{logLik()} and \code{summary()} methods. Set \code{marginal = FALSE}
to reproduce the likelihood default used by versions of \pkg{tempssm}
prior to this change.}
}
\value{
A list of named lists. Each element corresponds to one fold and contains
the output of \code{ts_cv_run_fold()}.
}
\description{
Apply \code{ts_cv_run_fold()} to multiple train/test splits and
collect the results. This function orchestrates time series
cross-validation but does not compute evaluation metrics.
}
\details{
The requested \pkg{future} execution plan is used only while folds are
evaluated. The plan that was active before calling this function is restored
on exit, including when fold evaluation raises an error.
}
\examples{
\dontrun{
data(yamaguchi_sst)

# 1. create train/test splits
folds <- ts_train_test_split(
  temp_data = yamaguchi_sst,
  initial   = 60,
  horizon   = 12,
  step      = 12
)

# 2. run cross-validation across all folds
cv_results <- ts_cv_run(
  folds,
  ar_order = 1,
  use_season = TRUE,
  parallel = TRUE
)

# inspect first fold result
cv_results[[1]]

# predicted vs observed for first fold
cbind(
  observed = as.numeric(cv_results[[1]]$y_test),
  predicted = as.numeric(cv_results[[1]]$y_pred)
)
}

}
