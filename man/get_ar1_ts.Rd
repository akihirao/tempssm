% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tempssm.R
\name{get_ar1_ts}
\alias{get_ar1_ts}
\title{Extract the smoothed first autoregressive component (AR1) as a time series}
\usage{
get_ar1_ts(res, ci = FALSE, ci_level = 0.95)
}
\arguments{
\item{res}{An object of class \code{"tempssm"} returned by \code{tempssm()}.}

\item{ci}{Logical; if TRUE, pointwise confidence intervals are returned.}

\item{ci_level}{Numeric confidence level between 0 and 1 (default: 0.95).}
}
\value{
A univariate \code{ts} object of the smoothed AR1 component
(in degrees Celsius).
If \code{ci = TRUE}, a multivariate \code{ts} object with columns
\code{ar1}, \code{lwr}, and \code{upr} is returned.
}
\description{
Extract the smoothed first autoregressive component (AR1) as a time series
}
\details{
The AR1 component represents short-term autocorrelated deviations
from the level and seasonal structure.
}
\examples{
\dontrun{
data(niigata_sst)
res <- tempssm(niigata_sst)
ar1_ts <- get_ar1_ts(res)
}
}
