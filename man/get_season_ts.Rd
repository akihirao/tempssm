% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tempssm.R, R/tempssm_accessors.R
\name{get_season_ts}
\alias{get_season_ts}
\title{Extract the smoothed seasonal component as a time series}
\usage{
get_season_ts(res, ci = FALSE, ci_level = 0.95)

get_season_ts(res, ci = FALSE, ci_level = 0.95)
}
\arguments{
\item{res}{An object of class \code{"tempssm"} returned by \code{tempssm()}.}

\item{ci}{Logical; if TRUE, pointwise confidence intervals are returned.}

\item{ci_level}{Numeric confidence level between 0 and 1 (default: 0.95).}
}
\value{
A univariate \code{ts} object of the smoothed seasonal component
(in degrees Celsius).
If \code{ci = TRUE}, a multivariate \code{ts} object with columns
\code{season}, \code{lwr}, and \code{upr} is returned.

A univariate \code{ts} object of the smoothed seasonal component
(in degrees Celsius).
If \code{ci = TRUE}, a multivariate \code{ts} object with columns
\code{season}, \code{lwr}, and \code{upr} is returned.
}
\description{
Extract the smoothed seasonal component as a time series

Extract the smoothed seasonal component as a time series
}
\details{
The seasonal component represents recurrent intra-year variability
captured by seasonal dummy state components in the state space model.

The seasonal component represents recurrent intra-year variability
captured by seasonal dummy state components in the state space model.
}
\examples{
\dontrun{
data(niigata_sst)
res <- tempssm(niigata_sst)
season_ts <- get_season_ts(res)
}
\dontrun{
data(niigata_sst)
res <- tempssm(niigata_sst)
season_ts <- get_season_ts(res)
}
}
